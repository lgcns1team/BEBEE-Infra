apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - ../../base
  - applicationset.yaml

labels:
  - pairs:
      environment: dev
      managed-by: argocd
