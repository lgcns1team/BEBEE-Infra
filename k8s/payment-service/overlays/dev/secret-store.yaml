apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: payment-service-sm
  namespace: bebee
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-northeast-2
      auth:
        jwt:
          serviceAccountRef:
            name: payment-service-secrets-sa
