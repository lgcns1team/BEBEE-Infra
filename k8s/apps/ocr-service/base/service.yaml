apiVersion: v1
kind: Service
metadata:
  name: ocr-service
  labels:
    app: ocr-service
    app.kubernetes.io/name: ocr-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app: ocr-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
