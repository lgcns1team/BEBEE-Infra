apiVersion: apps/v1
kind: Deployment
metadata:
  name: match-service
  namespace: bebee
spec:
  template:
    spec:
      containers:
        - name: match-service
          volumeMounts:
            - name: config
              mountPath: /app/config
      volumes:
        - name: config
          configMap:
            name: match-service-config
          
          
          
          # resources:
          #  requests:
          #    memory: "256Mi"
          #    cpu: "100m"
          #  limits:
          #    memory: "512Mi"
          #    cpu: "500m"