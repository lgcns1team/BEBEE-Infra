apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: bebee
spec:
  template:
    spec:
      containers:
      - name: notification-service
        volumeMounts:
          - name: config
            mountPath: /app/config
      volumes:
      - name: config
        configMap:
          name: notification-service-config
