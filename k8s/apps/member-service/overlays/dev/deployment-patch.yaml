apiVersion: apps/v1
kind: Deployment
metadata:
  name: member-service
  namespace: bebee
spec:
  template:
    spec:
      serviceAccountName: match-service-messaging-sa
      containers:
      - name: member-service
        volumeMounts:
          - name: config
            mountPath: /app/config
      volumes:
      - name: config
        configMap:
          name: member-service-config
